import { INavItems } from './Navigation';

const base: INavItems = {
  'id': 'about',
  'text': 'About',
  'link': '/about',
  'isAuth': false,
  'mobile': false
};

export const mockBaseNavProps = {
  base,
};
